# Copy to .env.local and fill in.
# Required for /api/analyze and /api/factcheck (and Whisper STT if used)
OPENAI_API_KEY=sk-...

# Optional — for authoritative-stats plugins (wired in later)
# INSEE_KEY=
# KOSIS_KEY=
